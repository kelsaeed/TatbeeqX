// Placeholder. The frontend has no widget-test suite by design — see
// docs/45-mobile-shell.md and the project memory's "no flutter_test in
// pubspec" note. UI changes are verified via `flutter analyze` (clean)
// plus operator smoke-tests on desktop and mobile.
//
// Delete this file if a real widget-test suite gets introduced; at that
// point also add `flutter_test` to dev_dependencies in pubspec.yaml.
void main() {}
